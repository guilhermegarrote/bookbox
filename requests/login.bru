meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost/bookbox/api/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{emailValido}}",
    "senha": "{{senhaValida}}"
  }
}
