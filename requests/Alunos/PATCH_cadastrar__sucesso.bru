meta {
  name: PATCH_cadastrar__sucesso
  type: http
  seq: 3
}

patch {
  url: http://localhost/bookbox/api/alunos/editar
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "id": "{{idAlunosValido}}",
     "nome": "{{nomeValido}}",
    "cpf": "{{cpfValido}}",
    "email": "{{emailValido}}",
    "telefone": "{{telefoneValido}}"
  }
}
