meta {
  name: POST_cadastrar__sucesso
  type: http
  seq: 1
}

post {
  url: http://localhost/bookbox/api/usuarios/cadastrar
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nome": "{{nome_valido}}",
    "email": "{{email_valido}}",
    "senha": "{{senha_valida}}",
    "senha_confirmada": "{{senha_valida}}"
  }
  
}
