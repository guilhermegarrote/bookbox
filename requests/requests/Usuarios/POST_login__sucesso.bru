meta {
  name: POST_login__sucesso
  type: http
  seq: 1
}

post {
  url: http://localhost/bookbox/api/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{email_valido}}",
    "senha": "{{senha_valida}}"
  }
  
}
