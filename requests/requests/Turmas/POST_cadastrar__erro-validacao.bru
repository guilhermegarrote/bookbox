meta {
  name: POST_cadastrar__erro-validacao
  type: http
  seq: 2
}

post {
  url: http://localhost/bookbox/api/turmas/cadastrar
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "curso": "{{curso_invalido}}",
    "horario": "{{horario_invalido}}",
    "regime": "{{regime_invalido}}",
    "data_inicio": "{{data_inicio_invalida}}",
    "data_fim": "{{data_fim_invalida}}"
  }
  
}
