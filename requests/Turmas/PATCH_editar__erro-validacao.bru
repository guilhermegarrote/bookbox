meta {
  name: PATCH_editar__erro-validacao
  type: http
  seq: 4
}

patch {
  url: http://localhost/bookbox/api/turmas/editar
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "id" : "{{idTurmasinValido}}",
    "curso": "{{cursoInvalido}}",
    "horario": "{{horarioInvalido}}",
    "regime": "{{regimeInvalido}}",
    "dataInicio": "{{dataInicioInvalida}}",
    "dataFim": "{{dataFimInvalida}}"
  }
  
}
